Example of Boundary Policy which allows to create IAM users and IAM role only with name "pb_*".

Prevents deletion of the Boundary Permissions.

Doesn't allow to edit or remove other IAM users or roles.
